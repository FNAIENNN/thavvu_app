name: thavvu_supervisor
description: Thavvu Supervisor Workflow App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  google_fonts: ^6.1.0
  intl: ^0.18.1
  image_picker: ^1.0.4
  provider: ^6.1.1
  go_router: ^12.1.1
  flutter_animate: ^4.3.0
  lucide_icons: ^0.257.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
